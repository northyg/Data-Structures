CC = g++
CPPFLAGS = -Wall -g -std=c++11

project3:		website.o hashtable.o website.h hashtable.h 

project3.o:		website.h

website.o:		hashtable.h

hashtable.o:


.PHONY:	clean
clean:			# clean the directory
			$(info -- cleaning the directory --)
			rm -f *.o
			rm -f project3
